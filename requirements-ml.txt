# Model TRAINING and evaluation only (src/classify/, notebooks).
# NOT required to run the scraper or the dashboard — kept out of
# requirements.txt so Streamlit Community Cloud does not try to install
# several GB of PyTorch on every deploy.
#
#   pip install -r requirements.txt -r requirements-ml.txt

scikit-learn==1.8.0
sentence-transformers==5.3.0
transformers==5.5.0
torch==2.11.0
matplotlib==3.10.7
seaborn==0.13.2
ipykernel==7.0.1
