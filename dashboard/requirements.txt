# Streamlit Cloud auto-discovers this file because it sits next to app.py, and
# PREFERS it over the repository root requirements.txt.
#
# It used to be a hand-maintained slim list, which silently drifted: psycopg was
# added to the root file for the Neon backend but not here, so every Cloud
# deploy failed with "ModuleNotFoundError: psycopg" while the root file looked
# correct. Rather than keep two lists in sync by hand, this now just includes
# the root one — pip resolves -r relative to this file.
#
# Add runtime dependencies to ../requirements.txt, never here.
-r ../requirements.txt
