# Streamlit Cloud auto-discovers this file because it sits next to app.py.
# Slim list: dashboard only — no torch/transformers (classification runs on the
# HF Space) and no scraping/serving deps. The root requirements.txt is the
# source of truth for the pipeline, notebooks, and HF Space build; the versions
# below are pinned to match it so the deployed dashboard is reproducible.
streamlit==1.56.0
pandas==2.3.3
supabase==2.28.0
anthropic==0.88.0
openai==2.30.0
python-dotenv==1.0.1
openpyxl==3.1.5
PyYAML==6.0.3
