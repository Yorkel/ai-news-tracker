# API-only dependencies - used by Dockerfile for the serving image.
# Keep minimal: only what src/serving/ needs at runtime.
# Versions pinned to the tested, working set (verified classifier.joblib loads + predicts).
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.12.5
numpy==2.3.4
scikit-learn==1.8.0
joblib==1.5.3
sentence-transformers==5.3.0
torch==2.11.0
prometheus-fastapi-instrumentator==7.1.0
prometheus-client==0.23.1   # imported directly by api.py's gated /metrics endpoint
