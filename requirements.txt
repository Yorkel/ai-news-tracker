# Runtime dependencies: the scraping pipeline, enrichment and the Streamlit
# dashboard. This is the file Streamlit Community Cloud installs, so it is
# deliberately kept small — the training stack (torch, transformers,
# sentence-transformers, scikit-learn) lives in requirements-ml.txt and is
# NOT needed to run the tracker. Installing it here would put the image
# several GB over Community Cloud's limits.

# ── Scraping and ingestion ───────────────────────────────────────────────
requests==2.32.5
feedparser==6.0.12            # RSS/Atom + Google Alert parsing (rss_adapter)
beautifulsoup4==4.14.2
lxml==6.1.1                   # tree builder for BeautifulSoup HTML scrapers
trafilatura==2.1.0            # main-article body extraction
python-dateutil==2.9.0.post0
PyYAML==6.0.3
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────────────────
# Two interchangeable backends behind the same query API (see
# src/scraping/pg_client.py): DATABASE_URL selects Postgres via psycopg,
# otherwise the Supabase client is used.
psycopg[binary]==3.3.4
supabase==2.28.0

# ── Enrichment ───────────────────────────────────────────────────────────
anthropic==0.88.0
openai==2.30.0

# ── Dashboard ────────────────────────────────────────────────────────────
streamlit==1.56.0
pandas==2.3.3
numpy==2.3.4
openpyxl==3.1.5               # .xlsx export on the Draft page
