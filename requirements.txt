# Runtime dependencies for the scraper, enrichment and Streamlit dashboard.
# Streamlit Community Cloud installs this file via dashboard/requirements.txt.

# ── Scraping and ingestion ───────────────────────────────────────────────
requests==2.32.5
feedparser==6.0.12            # RSS/Atom + Google Alert parsing (rss_adapter)
beautifulsoup4==4.14.2
lxml==6.1.1                   # tree builder for BeautifulSoup HTML scrapers
trafilatura==2.1.0            # main-article body extraction
python-dateutil==2.9.0.post0
PyYAML==6.0.3
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────────────────
# Two interchangeable backends behind the same query API (see
# src/scraping/pg_client.py): DATABASE_URL selects Postgres via psycopg,
# otherwise the Supabase client is used.
psycopg[binary]==3.3.4   # required by pg_client.py — the Postgres/Neon backend
supabase==2.28.0

# ── Enrichment ───────────────────────────────────────────────────────────
anthropic==0.88.0
openai==2.30.0

# ── Dashboard ────────────────────────────────────────────────────────────
streamlit==1.56.0
pandas==2.3.3
numpy==2.3.4
openpyxl==3.1.5               # .xlsx export on the Draft page

# ── FastAPI dashboard (web/) ─────────────────────────────────────────────
# Server-rendered replacement for the Streamlit app: a click is a form POST
# and a redirect, so nothing re-renders except the page requested.
fastapi==0.136.1
uvicorn[standard]==0.47.0
jinja2==3.1.6
python-multipart==0.0.20
itsdangerous==2.2.0        # signed session cookie in web/auth.py
